interface:
  display_name: "Image2 9格导演草图"
  short_description: "生成短生图稿并用事实门禁校验的 Image2 3x3 导演草图提示词"
  default_prompt: "Use $su-image9 v1.6.1 to create Image2 3x3 director blocking sketch storyboard prompts. Keep analysis detailed but final_image_prompts.md short: 2,500-4,500 characters per page, six blocks only (STYLE_LOCK, CANVAS_LOCK, REFERENCE_LOCK, CONTINUITY_LOCK, PANEL_TASKS P01-P09, NEGATIVE_LOCK). Build panel_plan.json first, then compile final prompts from it. Validate shot_data.json, panel_plan.json, and final prompts with the su-image9 validator before any image generation. Never manually compress prompts in chat for generation. Treat reference images only as silhouette/costume/prop ownership anchors, not as face, detail, or style targets."
