interface:
  display_name: "Image2 9格石墨分镜"
  short_description: "从已锁定分镜数据生成事实一致的3x3黑白石墨九格与外围标注"
  default_prompt: "Use $su-image9 with validated su-fenjingskill-zh shot_data.json. Preserve every source shot's order and camera composition, keep each page within one scene and one reality layer, use panel_plan.json as the only machine source, add only zero-fact-delta derived angles, compile the monochrome graphite canon, validate every artifact deterministically, and annotate generated PNG pages only outside the untouched source pixels. Never modify the director's storyboard table and never produce text-only or unlisted package artifacts."
